import Player from '@/game/Models/Player'
import Scene from '@/game'

export default function (this: Scene) {
  // console.log(this)
  // if (player) {
  //   player.update()

  //   // if (scene.time.now - scene.lastTime > scene.delayEmit) {
  //   //   scene.client.emit(scene.user)
  //   //   scene.lastTime = scene.time.now
  //   // }
  // }
}
